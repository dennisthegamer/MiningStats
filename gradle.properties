# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true

# IntelliJ IDEA is not yet fully compatible with configuration cache
org.gradle.configuration-cache=false

# Minecraft
# 1.21.5 is the last of the pre-1.21.6 GUI ABI (drawString returns int); range 1.21-1.21.5.
minecraft_version=1.21.5
minecraft_range=1.21-1.21.5

# Build plugins (Architectury template; dev.architectury.loom for <=1.21.8)
arch_plugin_version=3.4.164
arch_loom_version=1.11.454
shadow_version=9.5.1

# Fabric
loader_version=0.18.3
fabric_version=0.128.2+1.21.5

# NeoForge
neoforge_version=21.5.95

# Mod Properties
mod_version=1.3.0
maven_group=de.dennisthegamer.miningstats
archives_base_name=miningstats

# Dependencies
architectury_version=16.1.4
modmenu_version=14.0.2
# YACL pinned to 3.7.1+1.21.6 (old accessWidener) - compile-only; users install the
# matching 1.21.5 YACL. It cannot run on 1.21.5 (needs >=1.21.6), so no dev runtime.
yacl_version=3.7.1+1.21.6
